# راه‌اندازی Xray-core در X5.3-LevelUp — راهنمای قدم‌به‌قدم

این فاز طبق همون ترتیب امنی که قبلاً توافق کردیم پیاده‌سازی شده: **نصب + اتصال +
نمایش وضعیت**. هنوز چیزی به‌صورت عمومی (اینترنت) از طریق Xray سرو نمی‌شه — این
قدم بعدیه (Railway Networking چندپورتی) و عمداً اینجا انجام نشده تا ریسک صفر بمونه.

## چی اضافه شد

| فایل | نقش |
|---|---|
| `Dockerfile` | پایتون + باینری رسمی Xray-core + grpcurl را در یک کانتینر نصب می‌کند |
| `start.sh` | Xray را در پس‌زمینه (با retry خودکار در صورت crash) و uvicorn را در foreground اجرا می‌کند |
| `xray/config.json.template` | کانفیگ Xray: سه inbound — `vless-ws-xray` (پورت 12001)، `vless-grpc` (پورت 12002)، `vless-splithttp` (پورت 12003) + سرویس API روی 127.0.0.1 فقط |
| `xray_bridge.py` | پل پایتون↔Xray: وضعیت زنده، افزودن/حذف کاربر بدون ری‌استارت، خواندن مصرف |
| `main.py` (ویرایش) | هنگام ساخت/حذف کانفیگ، best-effort کاربر را هم در Xray سینک می‌کند + endpoint وضعیت |
| `pages.py` (ویرایش) | نمایش وضعیت Xray Engine و Mux در داشبورد (سبز/قرمز) |

**Reality اضافه نشده** — طبق درخواست قبلی‌تون.

## قدم ۱ — یک سرویس تستی جدا بساز (نه روی production)

دقیقاً طبق پلن قبلی: یک Environment یا سرویس جدید در Railway از همین ریپو بساز.
هیچ‌وقت این فاز رو مستقیم روی سرویس زنده امتحان نکن.

## قدم ۲ — نوع Builder سرویس تستی رو به Dockerfile تغییر بده

Railway → سرویس تستی → Settings → Build → از Nixpacks به **Dockerfile** سوییچ کن
(چون حالا یک `Dockerfile` در ریشه‌ی پروژه هست، Railway خودش تشخیص می‌ده).

## قدم ۳ — متغیرهای محیطی رو تنظیم کن

در همون سرویس تستی، این‌ها رو اضافه کن:

```
XRAY_ENABLED=true
XRAY_API_PORT=10085
XRAY_BRIDGE_TOKEN=<یک رشته‌ی تصادفی طولانی و مخفی خودت>
```

**درباره‌ی «توکن»:** خودِ gRPC API داخلی Xray هیچ مکانیزم توکن ندارد — امنیتش
اینه که فقط روی `127.0.0.1` (داخل همون کانتینر) گوش می‌ده، از بیرون اصلاً قابل
دسترسی نیست. `XRAY_BRIDGE_TOKEN` یک لایه‌ی امنیتی *سمت پنل پایتون* اضافه می‌کنه:
اگه بعداً خواستی یک endpoint مدیریتی حساس (مثل ری‌استارت دستی Xray) اضافه کنی،
این توکن رو چک می‌کنی — نه اینکه فکر کنی خودِ Xray یک لایه‌ی توکن‌دار داره.

## قدم ۴ — دیپلوی و بررسی لاگ‌ها

بعد از دیپلوی، تو لاگ‌های Railway باید این‌ها رو ببینی:
```
[start.sh] در حال اجرای Xray-core...
[start.sh] در حال اجرای پنل پایتون (uvicorn)...
```
اگه Xray کرش کنه، لاگش رو می‌بینی و بعد از ۵ ثانیه خودکار دوباره تلاش می‌کنه —
هیچ‌وقت کل کانتینر رو نمی‌خوابونه، چون uvicorn یک process کاملاً جدا و مستقله.

## قدم ۵ — تو داشبورد پنل چک کن

وارد پنل → داشبورد → کارت «وضعیت سرویس» شو. دو ردیف جدید می‌بینی:
- **Xray-core Engine** — اگه سبز و «متصل و پاسخگو» بود، یعنی API داخلی Xray
  (روی 127.0.0.1:10085) جواب می‌ده.
- **Mux (Multiplexing)** — فعلاً به همون وضعیت Xray Engine گره خورده (سبز/قرمز)؛
  چون mux به‌صورت پیش‌فرض تنظیم جداگانه‌ای در این تمپلیت نداره — اگه بخوای
  mux واقعی client-side رو هم اضافه کنیم (برای کاهش هندشیک روی شبکه‌های ناپایدار)،
  بگو تا در فاز بعدی جداگانه بهش بپردازیم.

می‌تونی مستقیم هم چک کنی: `GET /api/xray/status` (با کوکی ادمین لاگین‌شده).

## قدم ۶ — تست ساخت یک کانفیگ

از تب «کانفیگ‌ها» یک کانفیگ جدید بساز. اگه `XRAY_ENABLED=true` باشه، پنل
به‌صورت خودکار (fire-and-forget، بدون این‌که اگه Xray مشکلی داشت ساخت کانفیگ
اصلی fail بشه) همون UUID رو با `grpcurl` به هر سه inbound اضافه می‌کنه.

## اصول معرفی اندپوینت (Endpoint Introduction Principles)

هر endpoint جدیدی که به این پروژه اضافه می‌کنی، این چهار قاعده رو رعایت کن:

1. **جداسازی داخلی/بیرونی** — هر endpointـی که فقط باید بین process‌های خودِ
   کانتینر صحبت کنه (مثل API خود Xray) باید `listen: 127.0.0.1` باشه، نه
   `0.0.0.0`. فقط endpointـی که واقعاً باید از اینترنت قابل‌دسترس باشه
   `0.0.0.0` می‌گیره — و حتی اون‌وقت هم تا وقتی Railway Networking صریحاً
   براش پورت عمومی باز نکنی، از بیرون قابل‌دسترس نیست.
2. **Tag واضح و یکتا** — هر inbound/outbound Xray باید یک `tag` معنادار داشته
   باشه (`vless-ws-xray`, `vless-grpc`, ...) — چون هم روتینگ و هم آمار Stats
   API بر اساس همین تگ کار می‌کنه؛ تگ تکراری یا مبهم باعث خطای سایلنت می‌شه.
3. **Fail-soft، نه Fail-hard** — هر اتصال جدید بین پنل پایتون و یک سرویس
   بیرونی (Xray، Worker، ...) باید `try/except` best-effort باشه؛ خرابی یک
   سرویس کمکی هرگز نباید مسیر اصلی (ساخت/حذف کانفیگ، لاگین، relay) رو بشکنه —
   دقیقاً همون الگویی که تو `xray_bridge.py` پیاده شد.
4. **مستندسازی در همون فایل** — هر endpoint جدید باید بالای تعریفش توضیح بده
   *چرا* وجود داره، *کی* صداش می‌زنه، و *چه اتفاقی می‌افته اگه در دسترس نباشه* —
   دقیقاً همون کامنت‌هایی که در `main.py`/`xray_bridge.py` می‌بینی.

## فاز بعدی (وقتی خواستی)

وقتی از این فاز کاملاً مطمئن شدی (چند روز روی سرویس تستی، بدون crash)، فاز بعدی
اینه: باز کردن پورت‌های 12001–12003 روی Railway Networking (پورت عمومی جدا)،
و اضافه‌کردن گزینه‌های «VLESS+gRPC» و «VLESS+XHTTP بومی» به منوی ساخت کانفیگ
در `pages.py` (که واقعاً به این inboundها لینک بدن، نه فقط اسمی تو منو باشن).
